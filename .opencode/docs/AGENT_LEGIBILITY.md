# Agent Legibility

Future agents should leave a readable trail:

- cite files and paths instead of vague descriptions
- cite validation commands actually run
- note skipped checks and why they were skipped
- point to evidence paths for screenshots, logs, and verification notes
- call out residual risks instead of implying full completion

Keep summaries concise but auditable.
