# Quality Score

Use a simple pass/risk/blocker posture instead of vanity scoring.

Minimum bar before calling material work done:

- relevant validations executed or explicitly blocked with reason,
- evidence path recorded,
- security/release/UI review lanes used when triggered,
- `@quality-gate` signoff for material changes.
