# Evals

Replayability matters. Record enough context that another operator can rerun the same checks.

Prefer evidence artifacts under `.opencode/evidence/` for:

- validation commands,
- screenshots/UI evidence when relevant,
- smoke-test notes,
- release/restore rehearsal notes.

Use the release-gate and production-roadmap plans in `.opencode/plans/` as the current execution/eval baseline.
