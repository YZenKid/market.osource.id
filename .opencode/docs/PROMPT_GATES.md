# Prompt Gates

Keep prompts and agent instructions aligned with repo-local docs.

Do not invent new product behavior, security rules, or release claims when `README.md`, `PRD.md`, `TRD.md`, `ERD.md`, or `.opencode/plans/` already define them.

If a prompt conflicts with repo-local docs, resolve the conflict explicitly before implementation.
